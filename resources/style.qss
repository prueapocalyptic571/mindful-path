/* ── Global ────────────────────────────────────────────────── */
QMainWindow, QWidget {
    background-color: #faf8f3;
    color: #2c2416;
    font-family: "Segoe UI", "Ubuntu", "Noto Sans", "Helvetica Neue", Arial, sans-serif;
    font-size: 13px;
}

QDialog {
    background-color: #faf8f3;
    color: #2c2416;
}

QDialog QLabel {
    color: #2c2416;
}

QMessageBox {
    background-color: #faf8f3;
}

QMessageBox QLabel {
    color: #2c2416;
}

QMessageBox QPushButton {
    min-width: 80px;
}

/* ── Sidebar ───────────────────────────────────────────────── */
#sidebar {
    background-color: #251a14;
}

#sidebar_header {
    background-color: #1c120d;
    border-bottom: 1px solid #3a2820;
}

#sidebar_wheel {
    color: #c8790a;
    font-size: 30px;
    padding-bottom: 2px;
}

#sidebar_title {
    color: #ead8b8;
    font-size: 16px;
    font-weight: bold;
    letter-spacing: 1px;
}

#sidebar_sub {
    color: #7a6050;
    font-size: 10px;
    letter-spacing: 3px;
    text-transform: uppercase;
}

#sidebar_sep {
    color: #3a2820;
    max-width: 1px;
    background-color: #3a2820;
}

#nav_button {
    color: #a08878;
    background: transparent;
    border: none;
    border-radius: 7px;
    padding: 10px 14px;
    margin-bottom: 2px;
    text-align: left;
    font-size: 13px;
}

#nav_button:hover {
    background-color: #3a2820;
    color: #e8d8b8;
}

#nav_button:checked {
    background-color: #c8790a;
    color: #ffffff;
    font-weight: bold;
}

#sidebar_hint {
    color: #4a3020;
    font-size: 10px;
    font-style: italic;
}

#theme_toggle {
    background: transparent;
    color: #7a6050;
    border: 1px solid #3e2e22;
    border-radius: 14px;
    padding: 4px 12px;
    font-size: 11px;
}

#theme_toggle:hover {
    background: #3a2820;
    color: #d8c8a8;
    border-color: #5a4535;
}

/* ── View headers ──────────────────────────────────────────── */
#view_header {
    background-color: #faf8f3;
    border-bottom: 1px solid #ece8e0;
}

#view_title {
    font-size: 22px;
    font-weight: bold;
    color: #2c2416;
}

#view_sub {
    color: #8a7a6a;
    font-size: 12px;
    padding-bottom: 4px;
}

/* ── Today header ──────────────────────────────────────────── */
#today_header {
    background-color: #faf8f3;
    border-bottom: 1px solid #ece4d8;
}

#today_date {
    font-size: 20px;
    font-weight: bold;
    color: #2c2416;
}

#today_day {
    font-size: 13px;
    color: #8a7a6a;
}

/* ── Quote bar ─────────────────────────────────────────────── */
#quote_bar {
    background-color: #f4efe6;
    border-bottom: 1px solid #e8dece;
}

#quote_text {
    color: #5a4535;
    font-size: 13px;
    font-style: italic;
    line-height: 1.5;
}

#quote_author {
    color: #a09080;
    font-size: 11px;
    margin-top: 2px;
}

/* ── Cards / Rows ──────────────────────────────────────────── */
#habit_row, #stat_card, #reflect_card, #chart_card {
    background-color: #ffffff;
    border: 1px solid #ece8e0;
    border-radius: 10px;
}

#habit_row:hover {
    border-color: #d4c8b4;
}

#stat_card {
    min-width: 120px;
}

/* ── Empty label ───────────────────────────────────────────── */
#empty_label {
    color: #a09080;
    font-size: 13px;
    padding: 40px;
    qproperty-alignment: AlignCenter;
}

/* ── Checkboxes ────────────────────────────────────────────── */
QCheckBox {
    spacing: 8px;
}

QCheckBox::indicator {
    width: 20px;
    height: 20px;
    border: 2px solid #c8b898;
    border-radius: 5px;
    background: white;
}

QCheckBox::indicator:checked {
    background-color: #c8790a;
    border-color: #c8790a;
}

QCheckBox::indicator:hover {
    border-color: #c8790a;
}

/* ── Buttons ───────────────────────────────────────────────── */
QPushButton {
    background-color: #c8790a;
    color: white;
    border: none;
    border-radius: 7px;
    padding: 7px 18px;
    font-size: 12px;
    font-weight: 500;
}

QPushButton:hover {
    background-color: #d4880f;
}

QPushButton:pressed {
    background-color: #b06800;
}

QPushButton#secondary_btn {
    background-color: transparent;
    color: #8a7a6a;
    border: 1px solid #ddd5c5;
    font-weight: normal;
    padding: 6px 14px;
}

QPushButton#secondary_btn:hover {
    background-color: #f0ebe0;
    color: #4a3a2a;
    border-color: #c8b898;
}

/* ── Inputs ────────────────────────────────────────────────── */
QLineEdit, QTextEdit, QPlainTextEdit, QTimeEdit {
    background-color: #ffffff;
    border: 1px solid #d8d0c0;
    border-radius: 7px;
    padding: 8px 10px;
    color: #2c2416;
    selection-background-color: #c8790a;
    selection-color: white;
}

QLineEdit:focus, QTextEdit:focus, QPlainTextEdit:focus, QTimeEdit:focus {
    border-color: #c8790a;
    outline: none;
}

QTimeEdit::up-button, QTimeEdit::down-button {
    width: 16px;
    border: none;
    background: transparent;
}

/* ── Form layout labels ────────────────────────────────────── */
QFormLayout > QLabel, QFormLayout QLabel {
    color: #4a3a2a;
    font-size: 12px;
    font-weight: 500;
}

/* ── ComboBox ──────────────────────────────────────────────── */
QComboBox {
    background-color: #ffffff;
    border: 1px solid #d8d0c0;
    border-radius: 7px;
    padding: 6px 10px;
    color: #2c2416;
    min-width: 140px;
}

QComboBox:focus {
    border-color: #c8790a;
}

QComboBox::drop-down {
    border: none;
    padding-right: 8px;
}

QComboBox QAbstractItemView {
    background: #ffffff;
    border: 1px solid #d8d0c0;
    border-radius: 6px;
    selection-background-color: #c8790a22;
    selection-color: #2c2416;
}

/* ── Scroll areas ──────────────────────────────────────────── */
QScrollArea {
    background: transparent;
    border: none;
}

QScrollArea > QWidget > QWidget {
    background: transparent;
}

/* ── Scrollbars ────────────────────────────────────────────── */
QScrollBar:vertical {
    background: transparent;
    width: 8px;
    border-radius: 4px;
}

QScrollBar::handle:vertical {
    background: #d0c4b0;
    border-radius: 4px;
    min-height: 24px;
}

QScrollBar::handle:vertical:hover {
    background: #c8b898;
}

QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical {
    height: 0;
}

QScrollBar::add-page:vertical, QScrollBar::sub-page:vertical {
    background: transparent;
}

QScrollBar:horizontal {
    height: 0;
}

/* ── Dialog ────────────────────────────────────────────────── */
#dialog_title {
    font-size: 17px;
    font-weight: bold;
    color: #2c2416;
    padding-bottom: 4px;
}

/* ── Reflect ───────────────────────────────────────────────── */
#reflect_prompt {
    font-size: 14px;
    font-weight: bold;
    color: #4a3a2a;
}

#reflect_edit {
    background-color: #faf8f3;
    border: 1px solid #e0d8c8;
    border-radius: 6px;
}

/* ── ToolTip ───────────────────────────────────────────────── */
QToolTip {
    background-color: #2c2416;
    color: #f0e8d4;
    border: 1px solid #5a4535;
    border-radius: 5px;
    padding: 4px 8px;
    font-size: 12px;
}
